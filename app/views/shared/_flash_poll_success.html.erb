<%= "Poll created at '#{full_public_url(@poll)}'. 
You (or anyone else!) can edit it at '#{                                                                      full_edit_url(@poll)}', so keep it safe!" %>